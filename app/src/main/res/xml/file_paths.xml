<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Camera captures land in filesDir/images/<sessionId>/ and are handed to the
         camera app through FileProvider. -->
    <files-path name="session_images" path="images/" />
</paths>
